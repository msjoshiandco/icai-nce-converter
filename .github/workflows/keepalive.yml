name: keep-alive
on: {schedule: [{cron: "*/12 * * * *"}], workflow_dispatch: {}}
jobs: {ping: {runs-on: ubuntu-latest, steps: [{name: ping, run: "curl -fsS https://icai-nce-converter-eiul.onrender.com/health || true"}]}}
